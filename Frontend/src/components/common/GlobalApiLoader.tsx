import React from "react";

// Global floating loader has been removed as per requirement.
// Loaders now display directly inside the clicked action buttons.
export const GlobalApiLoader: React.FC = () => {
  return null;
};
